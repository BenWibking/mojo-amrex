"""Raw FFI surface for the AMReX Mojo C API.

The eventual implementation will keep all C ABI signatures in one place so the
higher-level wrappers can stay focused on ownership, validation, and user
ergonomics.
"""
